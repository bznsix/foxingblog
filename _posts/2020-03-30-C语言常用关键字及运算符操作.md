---
typora-root-url: C:\Users\Administrator\Desktop\我的博客
---

## C语言常用关键字及运算符操作

## c语言数据类型

### 1.sizeof

sizeof 是C 语言中已经定义好的关键字，他并不是函数

C操作对象：资源/内存（内存类型的资源，LCD缓存，LED灯）

C语言如何描述这些资源的属性？

资源属性【大小】

限制内存（土地）的大小，关键字 

int a;

sizeof(a)=4;//不同的编译器，不同的系统中由编译器决定

### 2.char

硬件芯片操作的最小单位

bit 1 0

软件操作的最小单位 8bit == 1Byte

4M  4Mbit kbit/s kb/s

char a; bit a;//一个在软件中定义 一个在硬件中定义

应用场景：

硬件处理的最小单位

char buff[xx];  int buff[x];

ASCII码表	8bit

### 3.int

大小：根据编译器决定

编译器最优的处理大小“

系统一个周期，所能接受的最大处理单位，int

32bit		4B		int

16bit		2B		int

====================================

整型常量

char a = 300;

2BIT		65535

int a =65535;

进制表示

3bit	8进制

111	0x7

1000	0x8		int a =010;//8

12	001 010

4bit	16进制	int a =0x10;//16

### 4.long short

特殊长度的限制符

###  5.unsigned signed

无符号：数据

有符号：数字

内存空间的最高字节是符号位还是数据

有符号位，无法进行右移操作

char a =-1;//0xff

无符号数可以进行右移

unsigned int a =-1;

### 6.float,double

大小

float	4B

double 	8B 

内存存在形式

0x10		16

0001 0000	16

1.0在内存中表示用float,double表示

1.0 1.0 	double

1.0f	float表示就够了

### 7.void

声明flag

void fun()	告诉函数没有返回值

### 自定义数据类型

####  struct

元素之间的和

```c
struct myabc{
    unsigned int a;
    unsigned int b;
    unsigned int c;
}; 

//使用时
struct myabc mybuf;//声明一个结构体
---------------
    顺序有要求
```

#### UNION 联合

共用起始地址的一段内存，技巧型代码

申请地址的区别：

![结构体和共用体区别](/../../../../../../pictures/C语言学习/结构体和共用体区别.png)

```c
union myabc{
char a;
int b;
}

union myabc abc;//调用
```

#### enum

列举 被命名的整型常数的集合

```c
#define MON 0
#define TUE 1
#define WED 2
enum abc{MOD=0,TUE,WED};//后面的会依次加一
```

#### typedef

数据类型的别名

```c
int a = 170;
int b = 3600;
//写下面通俗易懂
len_t a = 170;
time_t b = 3600;
--------------------
int a; //a是一个int类型的变量
typedef int a_t;//a是一个int类型的外号
a_t mysize;
```

#### 逻辑结构

多分枝

`switch{整数}`

#### do while for

while 条件

for 次数

do 做

### 类型修饰符

资源属性中位置的限定  

```c
auto char a;//自动在内存中寻找可读可写
			//区域如果在{}，就是在栈空间里
register int a;//限制变量定义在寄存器上的修饰符，定义快速访问的变量
			   //编译器会尽量存于寄存器，存不下时还是放在存储器中，&取地址对register无效
			   //内存（存储器） 寄存器（访问效率很高）
static 静态
应用场景：
    1），函数内部的变量
    int fun()
	{
    	int a ;=======>static int a;
	}
	2),函数外部的变量
    int a ;=====>static int a;
	int fun()
    {
        
    }
	3),函数的修饰符
    int fun();======>static int fun();
const 只读的变量 
常量的定义：
    const int a = 100;
volatile 告知编译器编译方法的关键字，不优化编译
    	 修饰变量的值的修改，不仅仅可以通过软件，也可以通过其他方式(硬件外部的用户)
    int a = 100;
	while(a==100);
	mylcd();
	
     
```

### 运算符

```c
CPU
* /
int a = b*10; //CPU可能多个周期，甚至软件模拟方法取实现乘法
int a = b+10;//一个周期可以处理
%：求模
取一个范围的数：
    eg:给一个任意的数字，得到一个1-100以内的数
        n % 100 得到 0-99 +1
得到M进制的一个个位数
循环的下标
        
逻辑运算
真 假
返回的结果就是 1 0
A||B //一个为真就为真 不能调换前后顺序 因为A判断为真就不判断B了
A$$B //两个都为真

!a //整个取反
~a//按位取反
        
位运算
左移：乘法 *2 
右移：
与符号变量有关(符号位会占位并不会位移)
int a; a>>n
unsigned int a; a>>n; 


&，|
A&0---->0
&：屏蔽
int a = 0x1234;
a & 0xff00;//屏蔽低八位
A&1----->A
&:取出
&:清零器

|：
A|0 ===A
保留
A|1 === 1
设置为高电平的方法，设置set
    
    
设置一个资源的bit5为高电平，其他位不变
int a;
a = a|1 0 0 0 0;//(0x1<<5) 

清除第五位
int a;
a = a & ~(0x1<<5); //方便移植

异或：^ 相异为1 常用于算法

```

